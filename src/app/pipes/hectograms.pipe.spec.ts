import { HectogramsPipe } from './hectograms.pipe';

describe('HectogramsPipe', () => {
  it('create an instance', () => {
    const pipe = new HectogramsPipe();
    expect(pipe).toBeTruthy();
  });
});
