import { DecimetersPipe } from './decimeters.pipe';

describe('DecimetersPipe', () => {
  it('create an instance', () => {
    const pipe = new DecimetersPipe();
    expect(pipe).toBeTruthy();
  });
});
